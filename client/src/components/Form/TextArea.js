import React from "react";

export const TextArea = props => (
  <div className="field">
  <div className="control">
    <textarea className="textarea is-info" {...props}></textarea>
  </div>
</div>
);
